import { ProjectCommentVote } from './project-comment-vote';

describe('ProjectCommentVote', () => {
  it('should create an instance', () => {
    expect(new ProjectCommentVote()).toBeTruthy();
  });
});
