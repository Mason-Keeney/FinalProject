import { ToolPresentPipe } from './tool-present.pipe';

describe('ToolPresentPipe', () => {
  it('create an instance', () => {
    const pipe = new ToolPresentPipe();
    expect(pipe).toBeTruthy();
  });
});
