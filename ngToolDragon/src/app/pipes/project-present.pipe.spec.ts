import { ProjectPresentPipe } from './project-present.pipe';

describe('ProjectPresentPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectPresentPipe();
    expect(pipe).toBeTruthy();
  });
});
