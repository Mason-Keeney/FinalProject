import { ToolSearchPipe } from './tool-search.pipe';

describe('ToolSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ToolSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
