import { UserPresentPipe } from './user-present.pipe';

describe('UserPresentPipe', () => {
  it('create an instance', () => {
    const pipe = new UserPresentPipe();
    expect(pipe).toBeTruthy();
  });
});
